#properties used by build-master.xml.
build.dir = ${basedir}/build
target.dir = ${build.dir}/target

build.root = ${build.dir}
integration.dir = ${build.root}/ivyrep

#default target to invoke in module builds
module.target = all
build.mode = online

#overridden by the build server at release time
module.status = milestone
build.number = 0
